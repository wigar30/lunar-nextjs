export type IconProps = {
  icon: keyof Icons
  className?: string
  tooltip?: string
}

export type Icons = {
  store: string
  storefront: string
  shopping_bag: string
  close: string
  person: string
  local_mall: string
  analytics: string
}
