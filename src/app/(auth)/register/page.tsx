import { PageWrapper } from "@/components/PageWrapper"

export default function Page() {
  return (
    <main className="w-full h-full min-h-screen bg-primary-100">
      <section className="w-full h-full min-h-screen">
        REGISTER
      </section>
    </main>
  )
}